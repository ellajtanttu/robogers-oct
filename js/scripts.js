// Business Logic:
function beepBoop(number) {
  let inputRange = [];
  const inputNumber = number
  for (let i = 0; i < inputNumber + 1; i++) {
    inputRange.push(i);
  }
  console.log(inputRange);
  return inputRange;
};

// UI Logic:
